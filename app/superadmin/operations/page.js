import PageClient from "./PageClient";

export const dynamic = "force-dynamic";
export const metadata = { title: "Operations — AapliSociety" };

export default function OperationsPage() {
  return <PageClient />;
}
