import PageClient from "./PageClient";

export const dynamic = "force-dynamic";
export const metadata = { title: "Platform settings — AapliSociety" };

export default function SettingsPage() {
  return <PageClient />;
}
